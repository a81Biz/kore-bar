{
  "name": "kore-bar-frontend-build",
  "private": true,
  "devDependencies": {
    "tailwindcss": "^3.4.1",
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/container-queries": "^0.1.1"
  },
  "scripts": {
    "build:css": "tailwindcss -i input.css -o style.css --minify"
  }
}
